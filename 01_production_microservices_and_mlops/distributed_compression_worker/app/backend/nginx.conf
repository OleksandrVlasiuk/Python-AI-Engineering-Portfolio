events {
    worker_connections 1000;
}

http {
    upstream backend {
        least_conn;
        server backend1:8082;
        server backend2:8083;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
}